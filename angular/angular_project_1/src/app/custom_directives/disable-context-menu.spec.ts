import { DisableContextMenu } from './disable-context-menu';

describe('DisableContextMenu', () => {
  it('should create an instance', () => {
    const directive = new DisableContextMenu();
    expect(directive).toBeTruthy();
  });
});
